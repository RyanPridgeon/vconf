spring:
  rabbitmq:
    addresses: localhost:5672
  redis:
    sentinel:
      master: mymaster
      nodes: localhost:26379
    
memcached:
  addresses: localhost:11211

server:
  port: ${port:9060}
  servlet-path: /management

management:
  port: 9160

customer:
  info:
    caller:
      endpoint: http://tpicxtmqa.uk.centricaplc.com:50000/XISOAPAdapter/MessageServlet?senderParty=Online&senderService=BSR_WebService_PI&receiverParty=&receiverService=&interface=PartyManagementManageCustomer24x7&interfaceNamespace=urn%3Abritishgas.co.uk%3AOnline%3AParty
      expiry: 3600
  ucrn:
    caller:
      endpoint: http://tpicxtmqa.uk.centricaplc.com:50000/XISOAPAdapter/MessageServlet?senderParty=&senderService=BSR_WebService_PI&receiverParty=&receiverService=&interface=PartyManagementCustomerIdentity24x7&interfaceNamespace=urn%3Abritishgas.co.uk%3AOnline%3AParty
      expiry: 3600
  change:
    caller:
      endpoint: http://tpicxtmqa.uk.centricaplc.com:50000/XISOAPAdapter/MessageServlet?senderParty=Online&senderService=BSR_WebService_PI&receiverParty=&receiverService=&interface=ManageOnlineCustomer&interfaceNamespace=urn%3Abritishgas.co.uk%3AOnline%3AParty
      expiry: 3600

webservice:
  authentication:
    username: ONLSRVUSER
    password: usatoday1

eureka:
  instance:
    metadata-map:
      instanceId: 1
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
    registry-fetch-interval-seconds: 5

hystrix:
  command:
    default:
      execution:
        isolation:
          strategy: THREAD
          thread:
            timeoutInMilliseconds: 30000

logging:
  level:
    org.springframework.ws.client.MessageTracing.sent: 'TRACE'
    org.springframework.ws.client.MessageTracing.received: 'TRACE'
    com.netflix.discovery: 'INFO'
    org.springframework.cloud: 'DEBUG'
